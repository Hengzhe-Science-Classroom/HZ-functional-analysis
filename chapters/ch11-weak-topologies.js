window.CHAPTERS = window.CHAPTERS || [];
window.CHAPTERS.push({
    id: 'ch11',
    number: 11,
    title: 'Weak & Weak* Topologies',
    subtitle: 'Coarser topologies for finer analysis',
    sections: [
        // ===== SECTION 1: Initial Topologies =====
        {
            id: 'initial-topologies',
            title: 'Initial Topologies',
            content: `
<div class="bridge-block chapter-opening">
<p><strong>Weaker Topologies for Stronger Theorems.</strong> In Chapter 10, we studied dual spaces in the norm topology. But the norm topology on infinite-dimensional spaces is "too strong" for many purposes: closed bounded sets are almost never compact. This chapter introduces weaker topologies, the weak topology on \(X\) and the weak* topology on \(X^*\), where compactness becomes available and many problems become tractable.</p>
</div>

<div class="bridge-block section-roadmap">
<p><strong>Section Roadmap.</strong> We begin with the general construction of initial topologies (the coarsest topology making a family of maps continuous). This abstract framework unifies the weak and weak* topologies as special cases.</p>
</div>

<h2>The Weakest Topology Making Functions Continuous</h2>

<p>Throughout analysis, we frequently encounter the following question: given a set \\(X\\) and a family of maps \\(f_\\alpha: X \\to Y_\\alpha\\) into topological spaces, what is the <em>coarsest</em> (weakest) topology on \\(X\\) making every \\(f_\\alpha\\) continuous? This is the <strong>initial topology</strong>, and it underlies all weak-type topologies in functional analysis.</p>

<div class="env-block definition">
    <div class="env-title">Definition 11.1 (Initial Topology)</div>
    <div class="env-body">
        <p>Let \\(X\\) be a set, \\(\\{(Y_\\alpha, \\tau_\\alpha)\\}_{\\alpha \\in A}\\) a family of topological spaces, and \\(f_\\alpha: X \\to Y_\\alpha\\) maps for each \\(\\alpha \\in A\\). The <strong>initial topology</strong> \\(\\sigma(X, \\{f_\\alpha\\})\\) on \\(X\\) is the weakest topology making every \\(f_\\alpha\\) continuous.</p>
        <p>Explicitly, \\(\\sigma(X, \\{f_\\alpha\\})\\) is generated by the subbasis:</p>
        \\[\\mathcal{S} = \\{f_\\alpha^{-1}(V) : \\alpha \\in A,\\; V \\in \\tau_\\alpha\\}\\]
    </div>
</div>

<div class="env-block remark">
    <div class="env-title">Remark (Subbasis to Basis)</div>
    <div class="env-body">
        <p>The open sets in the initial topology are arbitrary unions of finite intersections of subbasis elements. A typical basic open set has the form:</p>
        \\[f_{\\alpha_1}^{-1}(V_1) \\cap f_{\\alpha_2}^{-1}(V_2) \\cap \\cdots \\cap f_{\\alpha_n}^{-1}(V_n)\\]
        <p>where \\(\\alpha_1, \\ldots, \\alpha_n \\in A\\) and \\(V_i \\in \\tau_{\\alpha_i}\\). Crucially, only <em>finitely many</em> conditions appear in each basic open set.</p>
    </div>
</div>

<div class="env-block theorem">
    <div class="env-title">Proposition 11.2 (Universal Property)</div>
    <div class="env-body">
        <p>Let \\(X\\) carry the initial topology \\(\\sigma(X, \\{f_\\alpha\\})\\). A map \\(g: Z \\to X\\) from a topological space \\(Z\\) is continuous if and only if \\(f_\\alpha \\circ g: Z \\to Y_\\alpha\\) is continuous for every \\(\\alpha \\in A\\).</p>
    </div>
</div>

<div class="env-block proof">
    <div class="env-title">Proof</div>
    <div class="env-body">
        <p>(\\(\\Rightarrow\\)) If \\(g\\) is continuous, then \\(f_\\alpha \\circ g\\) is a composition of continuous maps.</p>
        <p>(\\(\\Leftarrow\\)) Suppose each \\(f_\\alpha \\circ g\\) is continuous. For any subbasis element \\(f_\\alpha^{-1}(V)\\), we have \\(g^{-1}(f_\\alpha^{-1}(V)) = (f_\\alpha \\circ g)^{-1}(V)\\), which is open in \\(Z\\). Since \\(g^{-1}\\) preserves finite intersections and arbitrary unions, \\(g\\) is continuous. \\(\\blacksquare\\)</p>
    </div>
</div>

<div class="env-block example">
    <div class="env-title">Example 11.3 (Product Topology as Initial Topology)</div>
    <div class="env-body">
        <p>The product topology on \\(\\prod_{\\alpha} X_\\alpha\\) is precisely the initial topology with respect to the projection maps \\(\\pi_\\alpha: \\prod X_\\alpha \\to X_\\alpha\\). The universal property above recovers the universal property of the product.</p>
    </div>
</div>

<div class="env-block example">
    <div class="env-title">Example 11.4 (Subspace Topology)</div>
    <div class="env-body">
        <p>If \\(A \\subseteq X\\), the subspace topology on \\(A\\) is the initial topology with respect to the single inclusion map \\(\\iota: A \\hookrightarrow X\\).</p>
    </div>
</div>

<div class="env-block theorem">
    <div class="env-title">Proposition 11.5 (Convergence in Initial Topologies)</div>
    <div class="env-body">
        <p>A net \\((x_\\lambda)\\) in \\(X\\) converges to \\(x\\) in the initial topology \\(\\sigma(X, \\{f_\\alpha\\})\\) if and only if \\(f_\\alpha(x_\\lambda) \\to f_\\alpha(x)\\) for every \\(\\alpha \\in A\\).</p>
    </div>
</div>

<div class="env-block remark">
    <div class="env-title">Remark (Coarseness Has Consequences)</div>
    <div class="env-body">
        <p>The initial topology has <em>fewer</em> open sets than finer topologies. This means:</p>
        <ul>
            <li>More nets converge (convergence is easier)</li>
            <li>Fewer sets are closed (closure is harder)</li>
            <li>More sets are compact (compactness is easier to achieve)</li>
        </ul>
        <p>This trade-off is the central theme of weak topologies in functional analysis.</p>
    </div>
</div>
            `,
            visualizations: [
                {
                    id: 'viz-initial-topology-subbasis',
                    title: 'Initial Topology: Subbasis and Basic Open Sets',
                    description: 'Visualize how preimages of open sets form a subbasis, and their finite intersections give the basis.',
                    setup: function(container) {
                        var viz = new VizEngine(container, { scale: 50, originX: 280, originY: 200 });
                        var t = 0;
                        var highlightIdx = 0;

                        viz.animate(function(time) {
                            t = time * 0.001;
                            viz.clear();
                            var ctx = viz.ctx;

                            // Draw X as a large region
                            ctx.fillStyle = '#1a1a40';
                            ctx.strokeStyle = viz.colors.blue;
                            ctx.lineWidth = 2;
                            ctx.beginPath();
                            ctx.ellipse(280, 200, 220, 160, 0, 0, Math.PI * 2);
                            ctx.fill(); ctx.stroke();
                            ctx.fillStyle = viz.colors.white;
                            ctx.font = 'bold 16px sans-serif';
                            ctx.textAlign = 'center';
                            ctx.fillText('X', 280, 30);

                            // f1^{-1}(V1) - a vertical strip
                            var alpha1 = 0.3 + 0.1 * Math.sin(t * 0.5);
                            ctx.fillStyle = viz.colors.blue + '25';
                            ctx.strokeStyle = viz.colors.blue;
                            ctx.lineWidth = 1.5;
                            ctx.setLineDash([5, 3]);
                            ctx.beginPath();
                            ctx.ellipse(200, 190, 90, 130, -0.2, 0, Math.PI * 2);
                            ctx.fill(); ctx.stroke();
                            ctx.setLineDash([]);
                            ctx.fillStyle = viz.colors.blue;
                            ctx.font = '13px sans-serif';
                            ctx.fillText('f\u2081\u207B\u00B9(V\u2081)', 140, 100);

                            // f2^{-1}(V2) - a horizontal strip
                            ctx.fillStyle = viz.colors.teal + '25';
                            ctx.strokeStyle = viz.colors.teal;
                            ctx.lineWidth = 1.5;
                            ctx.setLineDash([5, 3]);
                            ctx.beginPath();
                            ctx.ellipse(310, 170, 130, 80, 0.15, 0, Math.PI * 2);
                            ctx.fill(); ctx.stroke();
                            ctx.setLineDash([]);
                            ctx.fillStyle = viz.colors.teal;
                            ctx.font = '13px sans-serif';
                            ctx.fillText('f\u2082\u207B\u00B9(V\u2082)', 400, 100);

                            // Intersection region (basic open set)
                            var pulse = 0.6 + 0.2 * Math.sin(t * 2);
                            ctx.fillStyle = viz.colors.orange + '40';
                            ctx.strokeStyle = viz.colors.orange;
                            ctx.lineWidth = 2.5;
                            ctx.beginPath();
                            ctx.ellipse(255, 175, 60, 55, 0, 0, Math.PI * 2);
                            ctx.fill(); ctx.stroke();
                            ctx.fillStyle = viz.colors.orange;
                            ctx.font = 'bold 13px sans-serif';
                            ctx.fillText('f\u2081\u207B\u00B9(V\u2081) \u2229 f\u2082\u207B\u00B9(V\u2082)', 255, 240);
                            ctx.font = '11px sans-serif';
                            ctx.fillStyle = viz.colors.yellow;
                            ctx.fillText('Basic open set', 255, 258);

                            // Draw a point in the intersection
                            var px = 255 + 15 * Math.cos(t);
                            var py = 175 + 10 * Math.sin(t * 0.7);
                            ctx.fillStyle = viz.colors.white;
                            ctx.beginPath(); ctx.arc(px, py, 4, 0, Math.PI * 2); ctx.fill();
                            ctx.fillStyle = viz.colors.white;
                            ctx.font = '12px sans-serif';
                            ctx.fillText('x', px + 10, py - 8);

                            // Legend
                            ctx.font = '12px sans-serif';
                            ctx.textAlign = 'left';
                            ctx.fillStyle = viz.colors.text;
                            ctx.fillText('Subbasis: preimages of open sets', 20, 360);
                            ctx.fillText('Basis: finite intersections of subbasis elements', 20, 380);
                        });
                    }
                },
                {
                    id: 'viz-initial-vs-discrete',
                    title: 'Comparing Topologies: Initial vs Discrete vs Indiscrete',
                    description: 'See how the initial topology sits between the indiscrete and discrete topologies in terms of open sets.',
                    setup: function(container) {
                        var viz = new VizEngine(container, { scale: 40, originX: 280, originY: 200 });

                        function drawTopology(ctx, x, y, label, numSets, color) {
                            ctx.strokeStyle = color;
                            ctx.lineWidth = 2;
                            ctx.beginPath();
                            ctx.ellipse(x, y, 80, 60, 0, 0, Math.PI * 2);
                            ctx.stroke();
                            ctx.fillStyle = color;
                            ctx.font = 'bold 13px sans-serif';
                            ctx.textAlign = 'center';
                            ctx.fillText(label, x, y - 70);

                            // Draw nested open sets
                            for (var i = 0; i < numSets; i++) {
                                var r = 20 + i * 15;
                                var angle = (i * 2.3) % (Math.PI * 2);
                                var cx = x + (i % 3 - 1) * 18;
                                var cy = y + Math.floor(i / 3 - 0.5) * 18;
                                ctx.fillStyle = color + '20';
                                ctx.strokeStyle = color + '60';
                                ctx.lineWidth = 1;
                                ctx.beginPath();
                                ctx.ellipse(cx, cy, 15 + (i % 2) * 8, 12 + (i % 3) * 5, angle, 0, Math.PI * 2);
                                ctx.fill(); ctx.stroke();
                            }
                        }

                        viz.animate(function(time) {
                            viz.clear();
                            var ctx = viz.ctx;

                            ctx.fillStyle = viz.colors.white;
                            ctx.font = 'bold 15px sans-serif';
                            ctx.textAlign = 'center';
                            ctx.fillText('Lattice of Topologies on X', 280, 25);

                            // Indiscrete (fewest open sets)
                            drawTopology(ctx, 100, 300, 'Indiscrete', 1, viz.colors.red);
                            ctx.fillStyle = viz.colors.text;
                            ctx.font = '11px sans-serif';
                            ctx.fillText('{0, X} only', 100, 340);

                            // Initial topology (in between)
                            drawTopology(ctx, 280, 180, 'Initial / Weak', 4, viz.colors.orange);
                            ctx.fillStyle = viz.colors.text;
                            ctx.font = '11px sans-serif';
                            ctx.fillText('Preimages of open sets', 280, 220);

                            // Discrete (most open sets)
                            drawTopology(ctx, 460, 300, 'Discrete', 8, viz.colors.green);
                            ctx.fillStyle = viz.colors.text;
                            ctx.font = '11px sans-serif';
                            ctx.fillText('Every subset open', 460, 340);

                            // Arrows showing ordering
                            ctx.strokeStyle = viz.colors.text;
                            ctx.lineWidth = 1.5;
                            ctx.setLineDash([4, 3]);
                            // Indiscrete -> Initial
                            ctx.beginPath(); ctx.moveTo(150, 275); ctx.lineTo(220, 215); ctx.stroke();
                            // Initial -> Discrete
                            ctx.beginPath(); ctx.moveTo(340, 215); ctx.lineTo(410, 275); ctx.stroke();
                            ctx.setLineDash([]);

                            ctx.fillStyle = viz.colors.text;
                            ctx.font = '11px sans-serif';
                            ctx.fillText('\u2286', 180, 238);
                            ctx.fillText('\u2286', 380, 238);

                            ctx.fillStyle = viz.colors.yellow;
                            ctx.font = '12px sans-serif';
                            ctx.fillText('Coarser topology = fewer open sets = more compact sets', 280, 390);
                        });
                    }
                }
            ],
            exercises: [
                {
                    question: 'Let \\(X = \\mathbb{R}\\) and \\(f: \\mathbb{R} \\to \\mathbb{R}\\) be given by \\(f(x) = x^2\\). Describe the initial topology \\(\\sigma(\\mathbb{R}, \\{f\\})\\). Is it Hausdorff?',
                    hint: 'Consider which points can be separated. Note that \\(f(x) = f(-x)\\).',
                    solution: 'The subbasis consists of sets \\(f^{-1}(V) = \\{x : x^2 \\in V\\}\\) for \\(V\\) open in \\(\\mathbb{R}\\). Since \\(f(x) = f(-x)\\), the points \\(x\\) and \\(-x\\) cannot be separated by any open set in this topology (they have the same neighborhoods). Thus the topology is not Hausdorff for \\(x \\neq 0\\). Open sets are symmetric about the origin: if \\(U\\) is open and \\(x \\in U\\), then \\(-x \\in U\\).'
                },
                {
                    question: 'Prove that the initial topology is indeed the coarsest topology making all \\(f_\\alpha\\) continuous.',
                    hint: 'Any topology making \\(f_\\alpha\\) continuous must contain the subbasis elements.',
                    solution: 'Let \\(\\tau\\) be any topology on \\(X\\) making each \\(f_\\alpha\\) continuous. Then for every \\(\\alpha\\) and every open \\(V \\subseteq Y_\\alpha\\), we have \\(f_\\alpha^{-1}(V) \\in \\tau\\). Thus \\(\\tau\\) contains the subbasis \\(\\mathcal{S}\\). Since \\(\\tau\\) is a topology, it is closed under finite intersections and arbitrary unions, so it contains all sets generated by \\(\\mathcal{S}\\). Therefore \\(\\sigma(X, \\{f_\\alpha\\}) \\subseteq \\tau\\).'
                },
                {
                    question: 'Let \\((X, \\tau)\\) be a topological space and \\(\\{f_\\alpha\\}\\) a family that separates points (for every \\(x \\neq y\\), there exists \\(\\alpha\\) with \\(f_\\alpha(x) \\neq f_\\alpha(y)\\)). Show that \\(\\sigma(X, \\{f_\\alpha\\})\\) is Hausdorff if each \\(Y_\\alpha\\) is Hausdorff.',
                    hint: 'Use the separating property to find an \\(f_\\alpha\\) distinguishing two points, then pull back disjoint open sets.',
                    solution: 'Let \\(x \\neq y\\) in \\(X\\). Since the family separates points, there exists \\(\\alpha\\) with \\(f_\\alpha(x) \\neq f_\\alpha(y)\\). Since \\(Y_\\alpha\\) is Hausdorff, there exist disjoint open sets \\(U, V \\subseteq Y_\\alpha\\) with \\(f_\\alpha(x) \\in U\\) and \\(f_\\alpha(y) \\in V\\). Then \\(f_\\alpha^{-1}(U)\\) and \\(f_\\alpha^{-1}(V)\\) are disjoint open sets in \\(\\sigma(X, \\{f_\\alpha\\})\\) separating \\(x\\) and \\(y\\).'
                },
                {
                    question: 'Show that a net \\((x_\\lambda)_{\\lambda \\in \\Lambda}\\) converges to \\(x\\) in the initial topology \\(\\sigma(X, \\{f_\\alpha\\}_{\\alpha \\in A})\\) if and only if \\(f_\\alpha(x_\\lambda) \\to f_\\alpha(x)\\) for all \\(\\alpha \\in A\\).',
                    hint: 'Use the subbasis characterization of convergence: a net converges to \\(x\\) iff it is eventually in every subbasis element containing \\(x\\).',
                    solution: '(\\(\\Rightarrow\\)) If \\(x_\\lambda \\to x\\) and \\(V\\) is open in \\(Y_\\alpha\\) with \\(f_\\alpha(x) \\in V\\), then \\(f_\\alpha^{-1}(V)\\) is open in the initial topology containing \\(x\\), so \\(x_\\lambda\\) is eventually in \\(f_\\alpha^{-1}(V)\\), meaning \\(f_\\alpha(x_\\lambda)\\) is eventually in \\(V\\). (\\(\\Leftarrow\\)) Suppose \\(f_\\alpha(x_\\lambda) \\to f_\\alpha(x)\\) for all \\(\\alpha\\). Any basic open set containing \\(x\\) is \\(\\bigcap_{i=1}^n f_{\\alpha_i}^{-1}(V_i)\\) with \\(f_{\\alpha_i}(x) \\in V_i\\). Since each \\(f_{\\alpha_i}(x_\\lambda)\\) is eventually in \\(V_i\\), the net \\(x_\\lambda\\) is eventually in their finite intersection.'
                }
            ]
        },

        // ===== SECTION 2: Weak Topology on X =====
        {
            id: 'weak-topology',
            title: 'Weak Topology on X',
            content: `
<div class="bridge-block section-bridge">
<p><strong>Convergence Tested by Functionals.</strong> The weak topology on \(X\) is the coarsest topology making every functional \(f \in X^*\) continuous. Weak convergence \(x_n \rightharpoonup x\) means \(f(x_n) \to f(x)\) for all \(f \in X^*\). This is strictly weaker than norm convergence in infinite dimensions, and that is exactly its power.</p>
</div>

<div class="bridge-block section-roadmap">
<p><strong>Section Roadmap.</strong> We define the weak topology, characterize weakly open sets and weak convergence, prove that the weak topology is Hausdorff, and show that norm-closed convex sets are weakly closed (Mazur's theorem). We also establish weak lower semicontinuity of the norm.</p>
</div>

<h2>The Topology \\(\\sigma(X, X^*)\\)</h2>

<p>The most important instance of the initial topology in functional analysis arises when we use the dual space to define a topology on a normed space.</p>

<div class="env-block definition">
    <div class="env-title">Definition 11.6 (Weak Topology)</div>
    <div class="env-body">
        <p>Let \\(X\\) be a normed space with dual \\(X^*\\). The <strong>weak topology</strong> on \\(X\\), denoted \\(\\sigma(X, X^*)\\) or simply \\(w\\), is the initial topology generated by all bounded linear functionals \\(f \\in X^*\\).</p>
        <p>A subbasis for \\(\\sigma(X, X^*)\\) consists of sets of the form:</p>
        \\[\\{x \\in X : |f(x) - f(x_0)| < \\varepsilon\\}, \\quad f \\in X^*, \\; x_0 \\in X, \\; \\varepsilon > 0\\]
    </div>
</div>

<div class="env-block definition">
    <div class="env-title">Definition 11.7 (Weak Convergence)</div>
    <div class="env-body">
        <p>A sequence \\((x_n)\\) in \\(X\\) <strong>converges weakly</strong> to \\(x\\), written \\(x_n \\xrightarrow{w} x\\), if:</p>
        \\[f(x_n) \\to f(x) \\quad \\text{for every } f \\in X^*\\]
        <p>Equivalently, \\(x_n \\to x\\) in the topology \\(\\sigma(X, X^*)\\).</p>
    </div>
</div>

<div class="env-block remark">
    <div class="env-title">Remark (Strong vs Weak)</div>
    <div class="env-body">
        <p>Strong convergence (\\(\\|x_n - x\\| \\to 0\\)) always implies weak convergence, since \\(|f(x_n) - f(x)| \\leq \\|f\\| \\|x_n - x\\|\\). The converse fails in general in infinite-dimensional spaces.</p>
    </div>
</div>

<div class="env-block theorem">
    <div class="env-title">Theorem 11.8 (Properties of the Weak Topology)</div>
    <div class="env-body">
        <p>Let \\(X\\) be a normed space. Then:</p>
        <ol>
            <li>\\(\\sigma(X, X^*)\\) is Hausdorff (since \\(X^*\\) separates points of \\(X\\) by Hahn-Banach).</li>
            <li>\\(\\sigma(X, X^*) \\subseteq \\tau_{\\|\\cdot\\|}\\) (the weak topology is coarser than the norm topology).</li>
            <li>If \\(\\dim X = \\infty\\), then \\(\\sigma(X, X^*) \\subsetneq \\tau_{\\|\\cdot\\|}\\) (strictly coarser).</li>
            <li>A linear functional \\(\\varphi: X \\to \\mathbb{R}\\) is weakly continuous iff \\(\\varphi \\in X^*\\).</li>
        </ol>
    </div>
</div>

<div class="env-block proof">
    <div class="env-title">Proof (of (3))</div>
    <div class="env-body">
        <p>In an infinite-dimensional normed space, the open unit ball \\(B_X = \\{x : \\|x\\| < 1\\}\\) is not weakly open. Indeed, any weak neighborhood of \\(0\\) contains a set \\(\\bigcap_{i=1}^n \\ker(f_i)\\) for finitely many \\(f_i \\in X^*\\). Since \\(\\dim X = \\infty\\), the intersection \\(\\bigcap \\ker(f_i)\\) is a subspace of codimension \\(\\leq n\\), hence infinite-dimensional, and cannot be contained in \\(B_X\\). \\(\\blacksquare\\)</p>
    </div>
</div>

<div class="env-block example">
    <div class="env-title">Example 11.9 (Weak Convergence in \\(\\ell^2\\))</div>
    <div class="env-body">
        <p>The standard basis vectors \\(e_n \\in \\ell^2\\) satisfy \\(e_n \\xrightarrow{w} 0\\) but \\(\\|e_n\\| = 1 \\not\\to 0\\). Indeed, for any \\(f \\in (\\ell^2)^* \\cong \\ell^2\\), identified with some \\(y = (y_k)\\), we have \\(f(e_n) = y_n \\to 0\\) since \\(\\sum |y_k|^2 < \\infty\\).</p>
    </div>
</div>

<div class="env-block theorem">
    <div class="env-title">Theorem 11.10 (Weak Sequential Completeness)</div>
    <div class="env-body">
        <p>If \\(x_n \\xrightarrow{w} x\\) in a normed space \\(X\\), then:</p>
        <ol>
            <li>\\((\\|x_n\\|)\\) is bounded (by the Uniform Boundedness Principle).</li>
            <li>\\(\\|x\\| \\leq \\liminf_{n \\to \\infty} \\|x_n\\|\\) (the norm is weakly lower semicontinuous).</li>
        </ol>
    </div>
</div>

<div class="env-block proof">
    <div class="env-title">Proof (of (2))</div>
    <div class="env-body">
        <p>By Hahn-Banach, choose \\(f \\in X^*\\) with \\(\\|f\\| = 1\\) and \\(f(x) = \\|x\\|\\). Then \\(\\|x\\| = f(x) = \\lim f(x_n) \\leq \\liminf \\|f\\| \\|x_n\\| = \\liminf \\|x_n\\|\\). \\(\\blacksquare\\)</p>
    </div>
</div>

<div class="env-block example">
    <div class="env-title">Example 11.11 (Mazur's Theorem Preview)</div>
    <div class="env-body">
        <p>Mazur's theorem states that a convex set is weakly closed if and only if it is strongly closed. This remarkable fact shows that weak and strong closures of convex sets agree, even though the topologies differ vastly for non-convex sets.</p>
    </div>
</div>
            `,
            visualizations: [
                {
                    id: 'viz-weak-vs-strong-convergence',
                    title: 'Weak vs Strong Convergence in l\u00B2',
                    description: 'Compare how sequences converge weakly but not strongly. The standard basis in l\u00B2 converges weakly to 0.',
                    setup: function(container) {
                        var viz = new VizEngine(container, { scale: 35, originX: 300, originY: 220 });
                        var nSlider = { value: 1 };
                        var controls = document.createElement('div');
                        controls.style.cssText = 'display:flex;gap:12px;align-items:center;margin-top:8px;flex-wrap:wrap;';
                        container.appendChild(controls);
                        VizEngine.createSlider(controls, 'n = ', 1, 20, 1, 1, function(v) { nSlider.value = Math.round(v); });
                        var autoBtn = VizEngine.createButton(controls, 'Auto Advance', function() {
                            autoMode = !autoMode;
                            autoBtn.textContent = autoMode ? 'Stop' : 'Auto Advance';
                        });
                        var autoMode = false;
                        var autoTimer = 0;

                        viz.animate(function(time) {
                            var t = time * 0.001;
                            if (autoMode && t - autoTimer > 0.8) {
                                autoTimer = t;
                                nSlider.value = (nSlider.value % 20) + 1;
                            }

                            viz.clear();
                            var ctx = viz.ctx;
                            var n = nSlider.value;
                            var numBars = 12;

                            // Title
                            ctx.fillStyle = viz.colors.white;
                            ctx.font = 'bold 14px sans-serif';
                            ctx.textAlign = 'center';
                            ctx.fillText('Standard basis e_' + n + ' in l\u00B2', 300, 25);

                            // Draw coordinate bars for e_n
                            var barW = 30;
                            var barSpacing = 42;
                            var baseY = 280;
                            var maxH = 120;
                            var startX = 60;

                            for (var k = 1; k <= numBars; k++) {
                                var x = startX + (k - 1) * barSpacing;
                                var height = (k === n && n <= numBars) ? maxH : 0;

                                // Bar
                                if (height > 0) {
                                    ctx.fillStyle = viz.colors.blue;
                                    ctx.fillRect(x, baseY - height, barW, height);
                                    ctx.fillStyle = viz.colors.white;
                                    ctx.font = 'bold 12px sans-serif';
                                    ctx.textAlign = 'center';
                                    ctx.fillText('1', x + barW / 2, baseY - height - 10);
                                }

                                // Base tick
                                ctx.fillStyle = viz.colors.text;
                                ctx.font = '10px sans-serif';
                                ctx.textAlign = 'center';
                                ctx.fillText('k=' + k, x + barW / 2, baseY + 14);

                                // Zero line
                                ctx.strokeStyle = viz.colors.axis;
                                ctx.lineWidth = 0.5;
                                ctx.beginPath();
                                ctx.moveTo(x, baseY);
                                ctx.lineTo(x + barW, baseY);
                                ctx.stroke();
                            }

                            // If n > numBars, show dots
                            if (n > numBars) {
                                ctx.fillStyle = viz.colors.text;
                                ctx.font = '14px sans-serif';
                                ctx.textAlign = 'center';
                                ctx.fillText('... the 1 is at position ' + n + ' (off screen) ...', 300, baseY - 60);
                            }

                            // Baseline
                            ctx.strokeStyle = viz.colors.axis;
                            ctx.lineWidth = 1;
                            ctx.beginPath();
                            ctx.moveTo(50, baseY);
                            ctx.lineTo(560, baseY);
                            ctx.stroke();

                            // Info panel
                            ctx.fillStyle = viz.colors.yellow;
                            ctx.font = '13px sans-serif';
                            ctx.textAlign = 'left';
                            ctx.fillText('||e_' + n + '|| = 1  (always)', 30, 330);
                            ctx.fillStyle = viz.colors.teal;
                            ctx.fillText('For any y \u2208 l\u00B2: \u27E8e_' + n + ', y\u27E9 = y_' + n + ' \u2192 0', 30, 350);
                            ctx.fillStyle = viz.colors.orange;
                            ctx.fillText('Strong: e_n \u219B 0  |  Weak: e_n \u2192 0', 30, 370);
                        });
                    }
                },
                {
                    id: 'viz-weak-neighborhood',
                    title: 'Weak Neighborhoods vs Norm Balls',
                    description: 'Compare the shape of neighborhoods in the weak and norm topologies. Weak neighborhoods are infinite "slabs".',
                    setup: function(container) {
                        var viz = new VizEngine(container, { scale: 55, originX: 280, originY: 210 });
                        var fAngle = { value: 0.5 };
                        var controls = document.createElement('div');
                        controls.style.cssText = 'display:flex;gap:12px;align-items:center;margin-top:8px;flex-wrap:wrap;';
                        container.appendChild(controls);
                        VizEngine.createSlider(controls, 'Functional angle', 0, 3.14, 0.5, 0.05, function(v) { fAngle.value = v; });

                        viz.animate(function(time) {
                            viz.clear();
                            viz.drawGrid(1);
                            viz.drawAxes();
                            var ctx = viz.ctx;
                            var theta = fAngle.value;
                            var nx = Math.cos(theta);
                            var ny = Math.sin(theta);

                            // Norm ball (unit ball)
                            viz.drawCircle(0, 0, 1, viz.colors.blue + '15', viz.colors.blue, 1.5);

                            // Weak neighborhood: slab {x : |f(x)| < eps}
                            // f corresponds to direction (nx, ny), so level sets are lines perpendicular
                            var eps = 0.6;
                            var perpX = -ny;
                            var perpY = nx;
                            var ext = 8;

                            // Draw the slab
                            ctx.fillStyle = viz.colors.orange + '18';
                            ctx.beginPath();
                            var p1 = viz.toScreen(eps * nx + ext * perpX, eps * ny + ext * perpY);
                            var p2 = viz.toScreen(eps * nx - ext * perpX, eps * ny - ext * perpY);
                            var p3 = viz.toScreen(-eps * nx - ext * perpX, -eps * ny - ext * perpY);
                            var p4 = viz.toScreen(-eps * nx + ext * perpX, -eps * ny + ext * perpY);
                            ctx.moveTo(p1[0], p1[1]);
                            ctx.lineTo(p2[0], p2[1]);
                            ctx.lineTo(p3[0], p3[1]);
                            ctx.lineTo(p4[0], p4[1]);
                            ctx.closePath();
                            ctx.fill();

                            // Slab boundaries
                            viz.drawLine(eps * nx + ext * perpX, eps * ny + ext * perpY, eps * nx - ext * perpX, eps * ny - ext * perpY, viz.colors.orange, 1.5, true);
                            viz.drawLine(-eps * nx + ext * perpX, -eps * ny + ext * perpY, -eps * nx - ext * perpX, -eps * ny - ext * perpY, viz.colors.orange, 1.5, true);

                            // Functional direction arrow
                            viz.drawVector(0, 0, 2 * nx, 2 * ny, viz.colors.teal, 'f', 2);

                            // Origin point
                            viz.drawPoint(0, 0, viz.colors.white, '0', 4);

                            // Labels
                            ctx.fillStyle = viz.colors.blue;
                            ctx.font = '13px sans-serif';
                            ctx.textAlign = 'center';
                            ctx.fillText('Norm ball B(0,1)', 420, 350);
                            ctx.fillStyle = viz.colors.orange;
                            ctx.fillText('Weak nbhd {|f(x)| < \u03B5}', 420, 370);
                            ctx.fillStyle = viz.colors.text;
                            ctx.font = '11px sans-serif';
                            ctx.fillText('Weak nbhd extends to \u221E in directions \u22A5 f', 280, 395);
                        });
                    }
                },
                {
                    id: 'viz-weak-lsc-norm',
                    title: 'Weak Lower Semicontinuity of the Norm',
                    description: 'Visualize how ||x|| <= lim inf ||x_n|| when x_n converges weakly to x.',
                    setup: function(container) {
                        var viz = new VizEngine(container, { scale: 45, originX: 280, originY: 220 });

                        viz.animate(function(time) {
                            var t = time * 0.001;
                            viz.clear();
                            viz.drawGrid(1);
                            viz.drawAxes();
                            var ctx = viz.ctx;

                            // Sequence x_n: points on the unit circle that weakly converge to 0
                            // In R^2 this is not infinite-dimensional, so simulate conceptually
                            // Show points rotating on unit circle
                            var numPts = 8;
                            var phase = t * 0.3;

                            // Draw unit circle
                            viz.drawCircle(0, 0, 1, null, viz.colors.text + '40', 1);

                            // Draw norms as bars on the right
                            var barX = 430;
                            var barBaseY = 350;
                            var barScale = 80;

                            ctx.fillStyle = viz.colors.text;
                            ctx.font = '11px sans-serif';
                            ctx.textAlign = 'center';
                            ctx.fillText('||x_n|| values', barX + 40, barBaseY + 25);

                            for (var i = 0; i < numPts; i++) {
                                var angle = phase + i * Math.PI * 2 / numPts + i * 0.3;
                                var r = 1.0 + 0.3 * Math.sin(i * 1.7);
                                var px = r * Math.cos(angle);
                                var py = r * Math.sin(angle);
                                var alpha = Math.max(0.2, 1 - i * 0.1);

                                viz.drawPoint(px, py, viz.colors.blue, '', 4);

                                // Draw norm bar
                                var bx = barX + i * 12;
                                var barH = r * barScale;
                                ctx.fillStyle = viz.colors.blue + Math.round(alpha * 255).toString(16).padStart(2, '0');
                                ctx.fillRect(bx, barBaseY - barH, 8, barH);
                            }

                            // Limit point
                            var limNorm = 0.6;
                            var limX = limNorm * Math.cos(t * 0.2);
                            var limY = limNorm * Math.sin(t * 0.2);
                            viz.drawPoint(limX, limY, viz.colors.orange, 'x (weak limit)', 6);

                            // lim inf line
                            ctx.strokeStyle = viz.colors.orange;
                            ctx.lineWidth = 1.5;
                            ctx.setLineDash([4, 3]);
                            var limInfH = limNorm * barScale;
                            ctx.beginPath();
                            ctx.moveTo(barX - 10, barBaseY - limInfH);
                            ctx.lineTo(barX + numPts * 12 + 10, barBaseY - limInfH);
                            ctx.stroke();
                            ctx.setLineDash([]);
                            ctx.fillStyle = viz.colors.orange;
                            ctx.font = '11px sans-serif';
                            ctx.textAlign = 'right';
                            ctx.fillText('||x||', barX - 14, barBaseY - limInfH);

                            // Info
                            ctx.fillStyle = viz.colors.yellow;
                            ctx.font = '13px sans-serif';
                            ctx.textAlign = 'center';
                            ctx.fillText('||x|| \u2264 lim inf ||x_n||', 280, 20);
                            ctx.fillStyle = viz.colors.text;
                            ctx.font = '11px sans-serif';
                            ctx.fillText('Norm is weakly lower semicontinuous', 280, 40);
                        });
                    }
                }
            ],
            exercises: [
                {
                    question: 'Show that in a finite-dimensional normed space, the weak topology and the norm topology coincide.',
                    hint: 'Use the fact that in finite dimensions, all norms are equivalent and \\(X^*\\) has the same dimension as \\(X\\).',
                    solution: 'Let \\(\\dim X = n\\) and let \\(e_1, \\ldots, e_n\\) be a basis with dual basis \\(e_1^*, \\ldots, e_n^*\\). The weak topology is generated by \\(e_1^*, \\ldots, e_n^*\\). If \\(x_k \\xrightarrow{w} x\\), then \\(e_i^*(x_k) \\to e_i^*(x)\\) for all \\(i\\), which means every coordinate converges. Thus \\(\\|x_k - x\\| \\to 0\\) (all norms on finite-dimensional spaces are equivalent). So weak convergence implies strong convergence, and the topologies agree.'
                },
                {
                    question: 'Let \\(X = \\ell^p\\) with \\(1 < p < \\infty\\). Show that the sequence \\(x_n = e_1 + e_n\\) converges weakly to \\(e_1\\).',
                    hint: 'Use the Riesz representation: \\((\\ell^p)^* \\cong \\ell^q\\) where \\(1/p + 1/q = 1\\).',
                    solution: 'For any \\(f \\in (\\ell^p)^* \\cong \\ell^q\\) represented by \\(y = (y_k) \\in \\ell^q\\), we have \\(f(x_n) = f(e_1) + f(e_n) = y_1 + y_n\\). Since \\(y \\in \\ell^q\\), we have \\(y_n \\to 0\\), so \\(f(x_n) \\to y_1 = f(e_1)\\). Thus \\(x_n \\xrightarrow{w} e_1\\). Note \\(\\|x_n - e_1\\| = \\|e_n\\| = 1 \\not\\to 0\\), so the convergence is not strong.'
                },
                {
                    question: 'Prove that the unit ball \\(B_X = \\{x : \\|x\\| \\leq 1\\}\\) is weakly closed in any normed space \\(X\\).',
                    hint: 'Write \\(B_X\\) as an intersection of weakly closed sets using functionals.',
                    solution: 'For each \\(f \\in X^*\\) with \\(\\|f\\| \\leq 1\\), the set \\(\\{x : |f(x)| \\leq 1\\}\\) is weakly closed (being the preimage of \\([-1,1]\\) under the weakly continuous map \\(f\\)). Then \\(B_X = \\bigcap_{\\|f\\| \\leq 1} \\{x : |f(x)| \\leq 1\\}\\) by Hahn-Banach (if \\(\\|x\\| > 1\\), there exists \\(f\\) with \\(\\|f\\| = 1\\) and \\(f(x) = \\|x\\| > 1\\)). An intersection of weakly closed sets is weakly closed.'
                }
            ]
        },

        // ===== SECTION 3: Weak* Topology on X* =====
        {
            id: 'weak-star-topology',
            title: 'Weak* Topology on X*',
            content: `
<div class="bridge-block section-bridge">
<p><strong>An Even Weaker Topology on the Dual.</strong> On \(X^*\), we can define an even coarser topology: the weak* topology, where convergence means \(f_n(x) \to f(x)\) for every \(x \in X\). The payoff is the Banach-Alaoglu theorem: the closed unit ball of \(X^*\) is always weak*-compact, a result with no analog in the norm topology (in infinite dimensions).</p>
</div>

<div class="bridge-block section-roadmap">
<p><strong>Section Roadmap.</strong> We define the weak* topology, compare it with the weak and norm topologies on \(X^*\), prove the Banach-Alaoglu theorem (via Tychonoff's theorem), and explore its consequences for extracting convergent subsequences from bounded sequences of functionals.</p>
</div>

<h2>The Topology \\(\\sigma(X^*, X)\\)</h2>

<p>While the weak topology uses all of \\(X^*\\) to define convergence on \\(X\\), the <strong>weak* topology</strong> on \\(X^*\\) uses only the <em>evaluation functionals</em> from \\(X\\), rather than all of \\(X^{**}\\).</p>

<div class="env-block definition">
    <div class="env-title">Definition 11.12 (Weak* Topology)</div>
    <div class="env-body">
        <p>Let \\(X\\) be a normed space. The <strong>weak* topology</strong> (or \\(w^*\\)-topology) on \\(X^*\\), denoted \\(\\sigma(X^*, X)\\), is the initial topology generated by the evaluation maps:</p>
        \\[\\hat{x}: X^* \\to \\mathbb{R}, \\quad \\hat{x}(f) = f(x) \\quad \\text{for each } x \\in X\\]
        <p>Equivalently, it is the weakest topology on \\(X^*\\) making every evaluation functional \\(\\hat{x}\\) continuous.</p>
    </div>
</div>

<div class="env-block definition">
    <div class="env-title">Definition 11.13 (Weak* Convergence)</div>
    <div class="env-body">
        <p>A net \\((f_\\lambda)\\) in \\(X^*\\) <strong>converges weak*</strong> to \\(f\\), written \\(f_\\lambda \\xrightarrow{w^*} f\\), if:</p>
        \\[f_\\lambda(x) \\to f(x) \\quad \\text{for every } x \\in X\\]
        <p>This is precisely pointwise convergence of functionals on \\(X\\).</p>
    </div>
</div>

<div class="env-block remark">
    <div class="env-title">Remark (Three Topologies on \\(X^*\\))</div>
    <div class="env-body">
        <p>The dual space \\(X^*\\) carries three natural topologies, each progressively finer:</p>
        \\[\\sigma(X^*, X) \\subseteq \\sigma(X^*, X^{**}) \\subseteq \\tau_{\\|\\cdot\\|}\\]
        <p>That is: <strong>weak*</strong> \\(\\subseteq\\) <strong>weak</strong> \\(\\subseteq\\) <strong>norm</strong>. The weak* topology is in general strictly coarser than the weak topology on \\(X^*\\) (unless \\(X\\) is reflexive, in which case they coincide).</p>
    </div>
</div>

<div class="env-block theorem">
    <div class="env-title">Theorem 11.14 (Weak* Continuous Functionals)</div>
    <div class="env-body">
        <p>A linear functional \\(\\Phi: X^* \\to \\mathbb{R}\\) is \\(\\sigma(X^*, X)\\)-continuous if and only if there exists \\(x \\in X\\) such that \\(\\Phi(f) = f(x)\\) for all \\(f \\in X^*\\). That is, the weak*-continuous linear functionals on \\(X^*\\) are precisely the evaluation functionals \\(\\hat{x}\\).</p>
    </div>
</div>

<div class="env-block proof">
    <div class="env-title">Proof</div>
    <div class="env-body">
        <p>(\\(\\Leftarrow\\)) Each \\(\\hat{x}\\) is \\(w^*\\)-continuous by definition.</p>
        <p>(\\(\\Rightarrow\\)) Suppose \\(\\Phi\\) is \\(w^*\\)-continuous. Then \\(\\ker \\Phi\\) is \\(w^*\\)-closed. By continuity at \\(0\\), there exist \\(x_1, \\ldots, x_n \\in X\\) and \\(\\varepsilon > 0\\) such that \\(|f(x_i)| < \\varepsilon\\) for all \\(i\\) implies \\(|\\Phi(f)| < 1\\). This means \\(\\bigcap_{i=1}^n \\ker(\\hat{x}_i) \\subseteq \\ker \\Phi\\). By a lemma on linear functionals, \\(\\Phi = \\sum c_i \\hat{x}_i = \\widehat{\\sum c_i x_i}\\) for some scalars \\(c_i\\). \\(\\blacksquare\\)</p>
    </div>
</div>

<div class="env-block example">
    <div class="env-title">Example 11.15 (Weak* Convergence in \\(\\ell^1 \\cong (c_0)^*\\))</div>
    <div class="env-body">
        <p>Consider \\(c_0 = \\{(x_n) : x_n \\to 0\\}\\) with sup norm. Its dual is \\(\\ell^1\\). A sequence \\((y^{(k)})\\) in \\(\\ell^1\\) converges weak* to \\(y\\) iff:</p>
        \\[\\sum_{n=1}^\\infty y_n^{(k)} x_n \\to \\sum_{n=1}^\\infty y_n x_n \\quad \\text{for every } x \\in c_0\\]
        <p>Taking \\(x = e_m\\) shows \\(y_m^{(k)} \\to y_m\\) for each \\(m\\), i.e., coordinate-wise convergence is necessary (but not sufficient in general).</p>
    </div>
</div>

<div class="env-block theorem">
    <div class="env-title">Proposition 11.16 (Weak* is Strictly Coarser)</div>
    <div class="env-body">
        <p>If \\(X\\) is an infinite-dimensional normed space that is not reflexive, then \\(\\sigma(X^*, X) \\subsetneq \\sigma(X^*, X^{**})\\). That is, the weak* topology is strictly coarser than the weak topology on \\(X^*\\).</p>
    </div>
</div>
            `,
            visualizations: [
                {
                    id: 'viz-three-topologies',
                    title: 'Three Topologies on X*: Norm, Weak, Weak*',
                    description: 'Visualize the nesting of the norm, weak, and weak* topologies on the dual space.',
                    setup: function(container) {
                        var viz = new VizEngine(container, { scale: 40, originX: 280, originY: 200 });

                        viz.animate(function(time) {
                            var t = time * 0.001;
                            viz.clear();
                            var ctx = viz.ctx;

                            ctx.fillStyle = viz.colors.white;
                            ctx.font = 'bold 15px sans-serif';
                            ctx.textAlign = 'center';
                            ctx.fillText('Topologies on X* (from finest to coarsest)', 280, 25);

                            // Norm topology - smallest circle (most open sets = finest)
                            var cx = 280, cy = 210;

                            // Weak* open set (largest region)
                            ctx.fillStyle = viz.colors.teal + '15';
                            ctx.strokeStyle = viz.colors.teal;
                            ctx.lineWidth = 2;
                            ctx.beginPath();
                            ctx.ellipse(cx, cy, 200, 150, 0, 0, Math.PI * 2);
                            ctx.fill(); ctx.stroke();

                            // Weak open set
                            ctx.fillStyle = viz.colors.orange + '15';
                            ctx.strokeStyle = viz.colors.orange;
                            ctx.lineWidth = 2;
                            ctx.beginPath();
                            ctx.ellipse(cx, cy, 140, 105, 0, 0, Math.PI * 2);
                            ctx.fill(); ctx.stroke();

                            // Norm ball
                            ctx.fillStyle = viz.colors.blue + '20';
                            ctx.strokeStyle = viz.colors.blue;
                            ctx.lineWidth = 2;
                            ctx.beginPath();
                            ctx.ellipse(cx, cy, 70, 55, 0, 0, Math.PI * 2);
                            ctx.fill(); ctx.stroke();

                            // Labels
                            ctx.font = 'bold 13px sans-serif';
                            ctx.textAlign = 'center';

                            ctx.fillStyle = viz.colors.blue;
                            ctx.fillText('Norm ball', cx, cy - 5);
                            ctx.font = '11px sans-serif';
                            ctx.fillText('(finest)', cx, cy + 10);

                            ctx.font = 'bold 13px sans-serif';
                            ctx.fillStyle = viz.colors.orange;
                            ctx.fillText('Weak nbhd', cx, cy - 75);

                            ctx.fillStyle = viz.colors.teal;
                            ctx.fillText('Weak* nbhd', cx, cy - 125);
                            ctx.font = '11px sans-serif';
                            ctx.fillText('(coarsest)', cx, cy - 110);

                            // Convergence summary
                            ctx.font = '12px sans-serif';
                            ctx.textAlign = 'left';
                            ctx.fillStyle = viz.colors.blue;
                            ctx.fillText('||f_n - f|| \u2192 0', 30, 370);
                            ctx.fillStyle = viz.colors.text;
                            ctx.fillText('\u21D2', 170, 370);
                            ctx.fillStyle = viz.colors.orange;
                            ctx.fillText('\u03C6(f_n) \u2192 \u03C6(f) \u2200\u03C6 \u2208 X**', 190, 370);
                            ctx.fillStyle = viz.colors.text;
                            ctx.fillText('\u21D2', 380, 370);
                            ctx.fillStyle = viz.colors.teal;
                            ctx.fillText('f_n(x) \u2192 f(x) \u2200x \u2208 X', 400, 370);
                        });
                    }
                },
                {
                    id: 'viz-weak-star-convergence',
                    title: 'Weak* Convergence: Pointwise Agreement',
                    description: 'See how functionals converge weak* by checking pointwise convergence at each x in X.',
                    setup: function(container) {
                        var viz = new VizEngine(container, { scale: 40, originX: 80, originY: 300 });
                        var nSlider = { value: 3 };
                        var controls = document.createElement('div');
                        controls.style.cssText = 'display:flex;gap:12px;align-items:center;margin-top:8px;flex-wrap:wrap;';
                        container.appendChild(controls);
                        VizEngine.createSlider(controls, 'n = ', 1, 15, 3, 1, function(v) { nSlider.value = Math.round(v); });

                        viz.animate(function(time) {
                            viz.clear();
                            var ctx = viz.ctx;
                            var n = nSlider.value;

                            // Show functionals f_n(x) = sin(n*x)/n converging to f=0
                            ctx.fillStyle = viz.colors.white;
                            ctx.font = 'bold 14px sans-serif';
                            ctx.textAlign = 'center';
                            ctx.fillText('f_n(x) = sin(nx)/n  converging w* to f = 0', 300, 25);

                            viz.drawGrid(1);
                            viz.drawAxes();

                            // Draw limit function f = 0
                            viz.drawCurve(function(x) { return [x, 0]; }, -1, 12, 200, viz.colors.orange, 2.5);

                            // Draw f_n
                            viz.drawCurve(function(x) { return [x, Math.sin(n * x) / n]; }, -1, 12, 500, viz.colors.blue, 2);

                            // Draw evaluation at some test points
                            var testPoints = [1, 2, 3, 5, 7];
                            for (var i = 0; i < testPoints.length; i++) {
                                var xp = testPoints[i];
                                var yp = Math.sin(n * xp) / n;
                                viz.drawSegment(xp, 0, xp, yp, viz.colors.yellow, 1, true);
                                viz.drawPoint(xp, yp, viz.colors.blue, '', 3);
                                viz.drawPoint(xp, 0, viz.colors.orange, '', 3);
                            }

                            // Legend
                            ctx.font = '12px sans-serif';
                            ctx.textAlign = 'left';
                            ctx.fillStyle = viz.colors.blue;
                            ctx.fillText('f_' + n + '(x) = sin(' + n + 'x)/' + n, 350, 60);
                            ctx.fillStyle = viz.colors.orange;
                            ctx.fillText('f(x) = 0 (limit)', 350, 80);
                            ctx.fillStyle = viz.colors.yellow;
                            ctx.fillText('|f_n(x) - f(x)| \u2264 1/' + n + ' \u2192 0', 350, 100);
                            ctx.fillStyle = viz.colors.text;
                            ctx.font = '11px sans-serif';
                            ctx.fillText('Pointwise: f_n(x) \u2192 0 for each x', 350, 120);
                            ctx.fillText('But ||f_n|| does not \u2192 0 in general', 350, 138);
                        });
                    }
                }
            ],
            exercises: [
                {
                    question: 'Show that if \\(X\\) is reflexive, then the weak* topology \\(\\sigma(X^*, X)\\) coincides with the weak topology \\(\\sigma(X^*, X^{**})\\) on \\(X^*\\).',
                    hint: 'Use that reflexivity means \\(J: X \\to X^{**}\\) is surjective.',
                    solution: 'If \\(X\\) is reflexive, the canonical embedding \\(J: X \\to X^{**}\\) is surjective, so \\(X^{**} = J(X)\\). The weak* topology is generated by \\(\\{\\hat{x} : x \\in X\\}\\) and the weak topology by \\(\\{\\Phi : \\Phi \\in X^{**}\\}\\). Since every \\(\\Phi \\in X^{**}\\) equals \\(\\hat{x} = J(x)\\) for some \\(x \\in X\\), the two families of generating functionals are the same, so the topologies coincide.'
                },
                {
                    question: 'In \\(\\ell^\\infty = (\\ell^1)^*\\), show that the sequence \\(f_n = e_n^*\\) (evaluation at coordinate \\(n\\)) converges weak* to \\(0\\) but not weakly to \\(0\\).',
                    hint: 'For weak* convergence, test against elements of \\(\\ell^1\\). For weak convergence, find a functional in \\((\\ell^\\infty)^*\\) that does not vanish.',
                    solution: 'Weak*: For any \\(x = (x_k) \\in \\ell^1\\), \\(f_n(x) = x_n \\to 0\\) since \\(\\sum |x_k| < \\infty\\). So \\(f_n \\xrightarrow{w^*} 0\\). Not weakly: Consider the Banach limit \\(L \\in (\\ell^\\infty)^*\\), a continuous linear functional on \\(\\ell^\\infty\\) with \\(L((1,1,1,\\ldots)) = 1\\). The sequence \\((f_n(e_k))_k = (\\delta_{nk})_k\\) satisfies \\(L(f_n) = L(e_n)\\). Since \\(\\|e_n\\|_\\infty = 1\\) for all \\(n\\), we can choose \\(L\\) so that \\(L(e_n)\\) does not converge to \\(0\\) (e.g., using an ultrafilter limit). Thus \\(f_n\\) does not converge weakly to \\(0\\).'
                },
                {
                    question: 'Prove that weak* convergence \\(f_n \\xrightarrow{w^*} f\\) in \\(X^*\\) implies \\(\\|f\\| \\leq \\liminf \\|f_n\\|\\).',
                    hint: 'Use the same argument as for weak lower semicontinuity of the norm.',
                    solution: 'For any \\(x \\in X\\) with \\(\\|x\\| \\leq 1\\), we have \\(|f(x)| = \\lim |f_n(x)| \\leq \\liminf \\|f_n\\| \\cdot \\|x\\| \\leq \\liminf \\|f_n\\|\\). Taking the supremum over \\(\\|x\\| \\leq 1\\) gives \\(\\|f\\| = \\sup_{\\|x\\| \\leq 1} |f(x)| \\leq \\liminf \\|f_n\\|\\).'
                }
            ]
        },

        // ===== SECTION 4: Banach-Alaoglu Theorem =====
        {
            id: 'banach-alaoglu',
            title: 'Banach-Alaoglu Theorem',
            content: `
<div class="bridge-block section-bridge">
<p><strong>Compactness in Dual Spaces.</strong> Banach-Alaoglu gives weak*-compactness of the dual ball. Goldstine's theorem complements this by showing that the canonical image \(J(B_X)\) is weak*-dense in \(B_{X^{**}}\). Together, these results form the bridge to reflexivity theory in Chapter 12.</p>
</div>

<div class="bridge-block section-roadmap">
<p><strong>Section Roadmap.</strong> We prove Goldstine's theorem, connect weak*-density to reflexivity (\(X\) is reflexive if and only if \(J(B_X) = B_{X^{**}}\)), and discuss sequential versions of Banach-Alaoglu in separable spaces.</p>
</div>

<h2>Compactness in the Weak* Topology</h2>

<p>The Banach-Alaoglu theorem is one of the most important results in functional analysis. It provides a powerful source of compactness that compensates for the failure of the Heine-Borel theorem in infinite dimensions.</p>

<div class="env-block theorem">
    <div class="env-title">Theorem 11.17 (Banach-Alaoglu)</div>
    <div class="env-body">
        <p>Let \\(X\\) be a normed space. The closed unit ball \\(B_{X^*} = \\{f \\in X^* : \\|f\\| \\leq 1\\}\\) is compact in the weak* topology \\(\\sigma(X^*, X)\\).</p>
    </div>
</div>

<div class="env-block proof">
    <div class="env-title">Proof</div>
    <div class="env-body">
        <p>For each \\(x \\in X\\), let \\(D_x = [-\\|x\\|, \\|x\\|] \\subseteq \\mathbb{R}\\). Define the product space:</p>
        \\[P = \\prod_{x \\in X} D_x\\]
        <p>By Tychonoff's theorem, \\(P\\) is compact in the product topology.</p>
        <p>Define \\(\\Phi: B_{X^*} \\to P\\) by \\(\\Phi(f) = (f(x))_{x \\in X}\\). This is well-defined since \\(|f(x)| \\leq \\|f\\| \\|x\\| \\leq \\|x\\|\\) for \\(f \\in B_{X^*}\\).</p>
        <p><strong>Key observations:</strong></p>
        <ol>
            <li>\\(\\Phi\\) is a homeomorphism from \\((B_{X^*}, w^*)\\) onto its image \\(\\Phi(B_{X^*})\\) (since the weak* topology is precisely the topology of pointwise convergence).</li>
            <li>\\(\\Phi(B_{X^*})\\) is closed in \\(P\\): if a net \\((f(x))_{x \\in X}\\) converges in \\(P\\) to some \\((g(x))_{x \\in X}\\), then \\(g\\) is linear (limits of linear functions are linear) and \\(\\|g\\| \\leq 1\\) (since \\(|g(x)| \\leq \\|x\\|\\)).</li>
        </ol>
        <p>A closed subset of a compact space is compact, so \\(\\Phi(B_{X^*})\\) is compact, hence \\(B_{X^*}\\) is weak*-compact. \\(\\blacksquare\\)</p>
    </div>
</div>

<div class="env-block remark">
    <div class="env-title">Remark (Role of Tychonoff)</div>
    <div class="env-body">
        <p>The proof uses the axiom of choice (via Tychonoff's theorem). In fact, for <em>separable</em> spaces \\(X\\), Banach-Alaoglu can be proved without the axiom of choice, using sequential compactness and a diagonal argument.</p>
    </div>
</div>

<div class="env-block theorem">
    <div name="env-title">Corollary 11.18 (Sequential Banach-Alaoglu)</div>
    <div class="env-body">
        <p>If \\(X\\) is a separable normed space, then \\(B_{X^*}\\) is weak*-sequentially compact. That is, every sequence in \\(B_{X^*}\\) has a weak*-convergent subsequence.</p>
    </div>
</div>

<div class="env-block proof">
    <div class="env-title">Proof Sketch</div>
    <div class="env-body">
        <p>Let \\(\\{x_n\\}\\) be a countable dense subset of \\(X\\). Given a bounded sequence \\((f_k)\\) in \\(X^*\\), use a diagonal argument:</p>
        <ol>
            <li>\\((f_k(x_1))\\) is bounded, so extract a convergent subsequence.</li>
            <li>From that subsequence, extract a further subsequence with \\(f_k(x_2)\\) convergent.</li>
            <li>Continue and take the diagonal subsequence. It converges on all \\(x_n\\).</li>
            <li>By density and uniform boundedness, convergence extends to all \\(x \\in X\\). \\(\\blacksquare\\)</li>
        </ol>
    </div>
</div>

<div class="env-block example">
    <div class="env-title">Example 11.19 (Failure Without Weak*)</div>
    <div class="env-body">
        <p>In an infinite-dimensional normed space, the closed unit ball \\(B_X\\) is <em>never</em> compact in the norm topology (Riesz's lemma). Banach-Alaoglu recovers compactness by moving to the dual and using the weaker topology.</p>
    </div>
</div>

<div class="env-block example">
    <div class="env-title">Example 11.20 (Application: Existence of Minimizers)</div>
    <div class="env-body">
        <p>Banach-Alaoglu is fundamental for proving existence results. If \\((f_n)\\) is a bounded sequence in \\(X^*\\), then it has a weak*-convergent subnet (or subsequence if \\(X\\) is separable). This is used extensively in PDE theory, calculus of variations, and optimization.</p>
    </div>
</div>

<div class="env-block theorem">
    <div class="env-title">Proposition 11.21 (Metrizability)</div>
    <div class="env-body">
        <p>If \\(X\\) is separable, then the weak* topology on \\(B_{X^*}\\) is metrizable. A compatible metric is:</p>
        \\[d(f, g) = \\sum_{n=1}^{\\infty} \\frac{1}{2^n} \\frac{|f(x_n) - g(x_n)|}{1 + |f(x_n) - g(x_n)|}\\]
        <p>where \\(\\{x_n\\}\\) is a countable dense subset of \\(X\\).</p>
    </div>
</div>
            `,
            visualizations: [
                {
                    id: 'viz-banach-alaoglu-compactness',
                    title: 'Banach-Alaoglu: Weak* Compactness of the Dual Ball',
                    description: 'Visualize how a sequence of functionals in the dual ball has a weak*-convergent subsequence.',
                    setup: function(container) {
                        var viz = new VizEngine(container, { scale: 50, originX: 280, originY: 210 });
                        var step = { value: 0 };
                        var controls = document.createElement('div');
                        controls.style.cssText = 'display:flex;gap:12px;align-items:center;margin-top:8px;flex-wrap:wrap;';
                        container.appendChild(controls);
                        VizEngine.createSlider(controls, 'Step', 0, 30, 0, 1, function(v) { step.value = Math.round(v); });
                        VizEngine.createButton(controls, 'Reset', function() { step.value = 0; });

                        // Generate a sequence of "functionals" (as 2D points for illustration)
                        var pts = [];
                        var seed = 42;
                        function pseudoRandom() { seed = (seed * 1103515245 + 12345) & 0x7fffffff; return seed / 0x7fffffff; }
                        for (var i = 0; i < 30; i++) {
                            var angle = pseudoRandom() * Math.PI * 2;
                            var r = 0.3 + pseudoRandom() * 0.7;
                            pts.push({ x: r * Math.cos(angle), y: r * Math.sin(angle) });
                        }

                        // Compute subsequence converging to a cluster point
                        var target = { x: 0.3, y: 0.4 };
                        var subseq = pts.slice().sort(function(a, b) {
                            var da = (a.x - target.x) * (a.x - target.x) + (a.y - target.y) * (a.y - target.y);
                            var db = (b.x - target.x) * (b.x - target.x) + (b.y - target.y) * (b.y - target.y);
                            return da - db;
                        });

                        viz.animate(function(time) {
                            viz.clear();
                            viz.drawGrid(1);
                            viz.drawAxes();
                            var ctx = viz.ctx;
                            var s = step.value;

                            // Draw unit ball boundary
                            viz.drawCircle(0, 0, 1, null, viz.colors.text + '60', 1.5);
                            ctx.fillStyle = viz.colors.text;
                            ctx.font = '11px sans-serif';
                            ctx.textAlign = 'center';
                            ctx.fillText('B_{X*}', 55, 95);

                            // Draw all points
                            for (var i = 0; i < pts.length; i++) {
                                var alpha = (i < s) ? '40' : 'cc';
                                viz.drawPoint(pts[i].x, pts[i].y, viz.colors.blue + alpha, '', 3);
                            }

                            // Highlight subsequence points found so far
                            var numHighlight = Math.min(s, subseq.length);
                            for (var j = 0; j < numHighlight; j++) {
                                viz.drawPoint(subseq[j].x, subseq[j].y, viz.colors.orange, '' + (j + 1), 5);
                            }

                            // Draw limit point
                            if (s > 0) {
                                viz.drawPoint(target.x, target.y, viz.colors.green, 'f*', 6);
                                // Draw shrinking circle around limit
                                var radius = Math.max(0.05, 1.0 - s * 0.03);
                                viz.drawCircle(target.x, target.y, radius, null, viz.colors.green + '50', 1);
                            }

                            // Title and info
                            ctx.fillStyle = viz.colors.white;
                            ctx.font = 'bold 14px sans-serif';
                            ctx.textAlign = 'center';
                            ctx.fillText('Banach-Alaoglu: Extracting Convergent Subsequence', 280, 20);

                            ctx.font = '12px sans-serif';
                            ctx.fillStyle = viz.colors.text;
                            ctx.textAlign = 'left';
                            ctx.fillText('Every sequence in B_{X*} has a w*-convergent subsequence', 30, 385);
                            if (s > 5) {
                                ctx.fillStyle = viz.colors.green;
                                ctx.fillText('Subsequence clusters around limit f*', 30, 400);
                            }
                        });
                    }
                },
                {
                    id: 'viz-tychonoff-product',
                    title: 'Tychonoff Embedding: B_{X*} Inside a Product of Intervals',
                    description: 'See how the dual ball embeds into a product of compact intervals via the evaluation map.',
                    setup: function(container) {
                        var viz = new VizEngine(container, { scale: 40, originX: 280, originY: 200 });

                        viz.animate(function(time) {
                            var t = time * 0.001;
                            viz.clear();
                            var ctx = viz.ctx;

                            ctx.fillStyle = viz.colors.white;
                            ctx.font = 'bold 14px sans-serif';
                            ctx.textAlign = 'center';
                            ctx.fillText('Tychonoff Embedding for Banach-Alaoglu', 280, 22);

                            // Left: B_{X*}
                            ctx.strokeStyle = viz.colors.blue;
                            ctx.lineWidth = 2;
                            ctx.beginPath();
                            ctx.ellipse(120, 200, 70, 70, 0, 0, Math.PI * 2);
                            ctx.stroke();
                            ctx.fillStyle = viz.colors.blue + '15';
                            ctx.fill();
                            ctx.fillStyle = viz.colors.blue;
                            ctx.font = 'bold 14px sans-serif';
                            ctx.fillText('B_{X*}', 120, 140);

                            // A point f in B_{X*}
                            var fAngle = t * 0.4;
                            var fx = 120 + 35 * Math.cos(fAngle);
                            var fy = 200 + 35 * Math.sin(fAngle);
                            ctx.fillStyle = viz.colors.orange;
                            ctx.beginPath(); ctx.arc(fx, fy, 5, 0, Math.PI * 2); ctx.fill();
                            ctx.fillStyle = viz.colors.orange;
                            ctx.font = '12px sans-serif';
                            ctx.fillText('f', fx + 8, fy - 8);

                            // Arrow
                            ctx.strokeStyle = viz.colors.text;
                            ctx.lineWidth = 1.5;
                            ctx.beginPath(); ctx.moveTo(200, 200); ctx.lineTo(280, 200); ctx.stroke();
                            ctx.beginPath(); ctx.moveTo(280, 200); ctx.lineTo(272, 194); ctx.lineTo(272, 206); ctx.closePath(); ctx.fill();
                            ctx.fillStyle = viz.colors.text;
                            ctx.font = '12px sans-serif';
                            ctx.fillText('\u03A6', 240, 190);

                            // Right: Product of intervals
                            var numIntervals = 5;
                            var barW = 20;
                            var spacing = 45;
                            var startX = 310;
                            var barH = 120;
                            var baseY = 260;

                            for (var i = 0; i < numIntervals; i++) {
                                var bx = startX + i * spacing;
                                // Interval [-||x_i||, ||x_i||]
                                var norm = 0.5 + i * 0.3;
                                ctx.strokeStyle = viz.colors.teal;
                                ctx.lineWidth = 1.5;
                                ctx.beginPath();
                                ctx.moveTo(bx + barW / 2, baseY);
                                ctx.lineTo(bx + barW / 2, baseY - barH);
                                ctx.stroke();

                                // Endpoints
                                ctx.fillStyle = viz.colors.text;
                                ctx.font = '9px sans-serif';
                                ctx.textAlign = 'center';
                                ctx.fillText('-||x' + (i + 1) + '||', bx + barW / 2, baseY + 14);
                                ctx.fillText('||x' + (i + 1) + '||', bx + barW / 2, baseY - barH - 6);

                                // Point f(x_i) on the interval
                                var fVal = Math.sin(fAngle + i * 1.2) * 0.4 + 0.5;
                                var py = baseY - fVal * barH;
                                ctx.fillStyle = viz.colors.orange;
                                ctx.beginPath(); ctx.arc(bx + barW / 2, py, 4, 0, Math.PI * 2); ctx.fill();
                            }

                            ctx.fillStyle = viz.colors.text;
                            ctx.font = '10px sans-serif';
                            ctx.textAlign = 'center';
                            ctx.fillText('...', startX + numIntervals * spacing + 10, baseY - barH / 2);

                            ctx.fillStyle = viz.colors.teal;
                            ctx.font = 'bold 13px sans-serif';
                            ctx.fillText('\u220F D_{x}', startX + numIntervals * spacing / 2, 140);
                            ctx.font = '11px sans-serif';
                            ctx.fillText('(compact by Tychonoff)', startX + numIntervals * spacing / 2, 158);

                            // Bottom explanation
                            ctx.fillStyle = viz.colors.yellow;
                            ctx.font = '12px sans-serif';
                            ctx.textAlign = 'center';
                            ctx.fillText('\u03A6(f) = (f(x))_{x \u2208 X} maps B_{X*} into compact product', 280, 340);
                            ctx.fillStyle = viz.colors.text;
                            ctx.font = '11px sans-serif';
                            ctx.fillText('\u03A6 is a homeomorphism onto its (closed) image', 280, 360);
                        });
                    }
                }
            ],
            exercises: [
                {
                    question: 'Why does the Banach-Alaoglu theorem fail for the norm topology in infinite dimensions?',
                    hint: 'Use Riesz\'s lemma to show that \\(B_{X^*}\\) cannot be norm-compact.',
                    solution: 'By Riesz\'s lemma, if \\(X\\) is infinite-dimensional, we can construct a sequence \\((f_n)\\) in \\(B_{X^*}\\) with \\(\\|f_m - f_n\\| \\geq 1/2\\) for \\(m \\neq n\\). Such a sequence has no norm-convergent subsequence, so \\(B_{X^*}\\) is not norm-compact. The weak* topology has fewer open sets, making it easier to extract convergent subnets.'
                },
                {
                    question: 'Prove the sequential version of Banach-Alaoglu for \\(X = \\ell^1\\): every bounded sequence in \\((\\ell^1)^* = \\ell^\\infty\\) has a weak*-convergent subsequence.',
                    hint: 'Use a diagonal argument. \\(\\ell^1\\) is separable with dense subset consisting of finitely supported rational sequences.',
                    solution: 'Let \\((f_k)\\) be bounded in \\(\\ell^\\infty\\) with \\(\\|f_k\\|_\\infty \\leq M\\). For each standard basis vector \\(e_n \\in \\ell^1\\), \\((f_k(e_n))_k = ((f_k)_n)_k\\) is bounded by \\(M\\). Extract a subsequence with \\(f_k(e_1)\\) convergent. From that, extract a further subsequence with \\(f_k(e_2)\\) convergent. Take the diagonal subsequence \\((f_{k_j})\\): it converges on every \\(e_n\\), hence on every finitely supported sequence. Since these are dense in \\(\\ell^1\\) and \\(\\|f_{k_j}\\| \\leq M\\), convergence extends to all of \\(\\ell^1\\) by a standard \\(\\varepsilon/3\\) argument.'
                },
                {
                    question: 'Show that \\(B_{X^*}\\) with the weak* topology is metrizable if and only if \\(X\\) is separable.',
                    hint: 'One direction uses the metric \\(d(f,g) = \\sum 2^{-n} |f(x_n) - g(x_n)| / (1 + |f(x_n) - g(x_n)|)\\). For the other, show that if \\(B_{X^*}\\) is metrizable, then \\(X\\) is separable.',
                    solution: '(\\(\\Rightarrow\\)) If \\(B_{X^*}\\) is \\(w^*\\)-metrizable, it is second countable (compact metrizable spaces are). The \\(w^*\\)-topology on \\(B_{X^*}\\) is generated by \\(\\{\\hat{x}|_{B_{X^*}} : x \\in X\\}\\). If a countable subfamily suffices, say \\(\\{\\hat{x}_n\\}\\), then \\(\\overline{\\operatorname{span}}\\{x_n\\}\\) separates points of \\(B_{X^*}\\), and by Hahn-Banach this span is dense in \\(X\\). (\\(\\Leftarrow\\)) If \\(X\\) is separable with dense \\(\\{x_n\\}\\), the given metric generates the \\(w^*\\)-topology on \\(B_{X^*}\\).'
                },
                {
                    question: 'Use Banach-Alaoglu to prove: every bounded sequence in a reflexive Banach space has a weakly convergent subsequence.',
                    hint: 'Reflexivity means \\(J: X \\to X^{**}\\) is surjective, so weak and weak* topologies on \\(X^{**}\\) restricted to \\(J(X)\\) can be related.',
                    solution: 'Let \\((x_n)\\) be bounded in \\(X\\). View \\(J(x_n) \\in X^{**}\\). Since \\(X\\) is reflexive, \\(X^{**} = J(X)\\), and the weak topology on \\(X\\) (via \\(J\\)) corresponds to the weak* topology on \\(X^{**} = (X^*)^*\\). By Banach-Alaoglu, \\(B_{X^{**}}\\) is \\(w^*\\)-compact. If \\(X\\) is also separable, this is sequentially compact, giving a subsequence \\(J(x_{n_k}) \\xrightarrow{w^*} \\Phi \\in X^{**}\\). Since \\(X\\) is reflexive, \\(\\Phi = J(x)\\) for some \\(x \\in X\\), so \\(x_{n_k} \\xrightarrow{w} x\\). The general (non-separable) case uses the Eberlein-Smulian theorem.'
                }
            ]
        },

        // ===== SECTION 5: Goldstine's Theorem =====
        {
            id: 'goldstine-theorem',
            title: "Goldstine's Theorem",
            content: `
<div class="bridge-block section-bridge">
<p><strong>Connecting Compactness Notions.</strong> In metric spaces, compactness and sequential compactness are equivalent. In weak topologies, this equivalence is far from obvious. The Eberlein-Smulian theorem resolves this: a subset of a Banach space is weakly compact if and only if it is weakly sequentially compact.</p>
</div>

<div class="bridge-block section-roadmap">
<p><strong>Section Roadmap.</strong> We state the Eberlein-Smulian theorem, explore its implications for extracting weakly convergent subsequences, and use it to characterize reflexive spaces. We also revisit Goldstine's theorem in light of these results.</p>
</div>

<div class="bridge-block chapter-closing">
<p><strong>Looking Ahead.</strong> The weak and weak* topologies have given us compactness tools unavailable in the norm topology. In Chapter 12, we study reflexive spaces, where the canonical embedding \(J: X \to X^{**}\) is surjective, unlocking the full power of weak compactness via the Eberlein-Smulian theorem.</p>
</div>

<h2>Density of \\(J(B_X)\\) in \\(B_{X^{**}}\\)</h2>

<p>Goldstine's theorem reveals a fundamental relationship between a Banach space and its bidual. While the canonical embedding \\(J: X \\to X^{**}\\) is always an isometry, Goldstine's theorem tells us that the image of the unit ball is always <em>dense</em> in the bidual ball, regardless of whether \\(X\\) is reflexive.</p>

<div class="env-block definition">
    <div class="env-title">Definition 11.22 (Canonical Embedding)</div>
    <div class="env-body">
        <p>The <strong>canonical embedding</strong> \\(J: X \\to X^{**}\\) is defined by:</p>
        \\[J(x)(f) = f(x) \\quad \\text{for all } f \\in X^*, \\; x \\in X\\]
        <p>By the Hahn-Banach theorem, \\(J\\) is an isometric embedding: \\(\\|J(x)\\| = \\|x\\|\\) for all \\(x \\in X\\).</p>
    </div>
</div>

<div class="env-block theorem">
    <div class="env-title">Theorem 11.23 (Goldstine's Theorem)</div>
    <div class="env-body">
        <p>Let \\(X\\) be a normed space. Then \\(J(B_X)\\) is weak*-dense in \\(B_{X^{**}}\\). That is:</p>
        \\[\\overline{J(B_X)}^{w^*} = B_{X^{**}}\\]
    </div>
</div>

<div class="env-block proof">
    <div class="env-title">Proof</div>
    <div class="env-body">
        <p>Let \\(\\Phi \\in B_{X^{**}}\\) and let \\(U\\) be a \\(w^*\\)-neighborhood of \\(\\Phi\\). We must show \\(U \\cap J(B_X) \\neq \\emptyset\\).</p>
        <p>A basic \\(w^*\\)-neighborhood of \\(\\Phi\\) has the form:</p>
        \\[U = \\{\\Psi \\in X^{**} : |\\Psi(f_i) - \\Phi(f_i)| < \\varepsilon, \\; i = 1, \\ldots, n\\}\\]
        <p>for some \\(f_1, \\ldots, f_n \\in X^*\\) and \\(\\varepsilon > 0\\). We need to find \\(x \\in B_X\\) such that \\(|f_i(x) - \\Phi(f_i)| < \\varepsilon\\) for all \\(i\\).</p>
        <p>Consider the map \\(T: X \\to \\mathbb{R}^n\\) given by \\(T(x) = (f_1(x), \\ldots, f_n(x))\\). We claim that \\((\\Phi(f_1), \\ldots, \\Phi(f_n)) \\in \\overline{T(B_X)}\\).</p>
        <p>Suppose not. Then by the (finite-dimensional) Hahn-Banach separation theorem, there exists \\(c = (c_1, \\ldots, c_n)\\) with:</p>
        \\[\\sum c_i \\Phi(f_i) > \\sup_{x \\in B_X} \\sum c_i f_i(x)\\]
        <p>Let \\(g = \\sum c_i f_i \\in X^*\\). Then \\(\\Phi(g) > \\sup_{\\|x\\| \\leq 1} g(x) = \\|g\\|\\). But \\(|\\Phi(g)| \\leq \\|\\Phi\\| \\|g\\| \\leq \\|g\\|\\), contradiction. \\(\\blacksquare\\)</p>
    </div>
</div>

<div class="env-block theorem">
    <div class="env-title">Corollary 11.24 (Reflexivity Characterization)</div>
    <div class="env-body">
        <p>A Banach space \\(X\\) is reflexive if and only if \\(B_X\\) is weakly compact.</p>
    </div>
</div>

<div class="env-block proof">
    <div class="env-title">Proof</div>
    <div class="env-body">
        <p>(\\(\\Rightarrow\\)) If \\(X\\) is reflexive, \\(J(B_X) = B_{X^{**}}\\) is \\(w^*\\)-compact (Banach-Alaoglu), and the weak topology on \\(X\\) equals the \\(w^*\\)-topology on \\(X^{**}\\) restricted to \\(J(X)\\).</p>
        <p>(\\(\\Leftarrow\\)) If \\(B_X\\) is weakly compact, then \\(J(B_X)\\) is \\(w^*\\)-compact (since \\(J\\) is \\(w\\)-to-\\(w^*\\) continuous), hence \\(w^*\\)-closed. By Goldstine, \\(J(B_X)\\) is \\(w^*\\)-dense in \\(B_{X^{**}}\\), so \\(J(B_X) = B_{X^{**}}\\), proving \\(J\\) is surjective. \\(\\blacksquare\\)</p>
    </div>
</div>

<div class="env-block example">
    <div class="env-title">Example 11.25 (Goldstine for \\(c_0\\))</div>
    <div class="env-body">
        <p>For \\(X = c_0\\), we have \\(X^* = \\ell^1\\) and \\(X^{**} = \\ell^\\infty\\). The canonical embedding sends \\(x = (x_n) \\in c_0\\) to itself viewed in \\(\\ell^\\infty\\). Goldstine says that \\(B_{c_0}\\) (sequences converging to \\(0\\) with sup-norm \\(\\leq 1\\)) is \\(w^*\\)-dense in \\(B_{\\ell^\\infty}\\) (all bounded sequences with sup-norm \\(\\leq 1\\)). This makes sense: any bounded sequence can be approximated pointwise by sequences that eventually vanish.</p>
    </div>
</div>

<div class="env-block theorem">
    <div class="env-title">Corollary 11.26 (Density of \\(J(X)\\))</div>
    <div class="env-body">
        <p>For any normed space \\(X\\), \\(J(X)\\) is \\(w^*\\)-dense in \\(X^{**}\\).</p>
    </div>
</div>

<div class="env-block remark">
    <div class="env-title">Remark (Non-Reflexive Spaces)</div>
    <div class="env-body">
        <p>For non-reflexive spaces like \\(c_0\\) or \\(\\ell^1\\), Goldstine tells us that \\(J(B_X)\\) is dense but not closed in the weak* topology. The "gap" between \\(J(B_X)\\) and \\(B_{X^{**}}\\) measures the failure of reflexivity. This gap is formalized by James's theorem: \\(X\\) is reflexive iff every \\(f \\in X^*\\) attains its norm on \\(B_X\\).</p>
    </div>
</div>
            `,
            visualizations: [
                {
                    id: 'viz-goldstine-density',
                    title: "Goldstine's Theorem: J(B_X) Dense in B_{X**}",
                    description: 'Visualize how the image of the unit ball under the canonical embedding is weak*-dense in the bidual ball.',
                    setup: function(container) {
                        var viz = new VizEngine(container, { scale: 40, originX: 280, originY: 210 });
                        var numPts = { value: 5 };
                        var controls = document.createElement('div');
                        controls.style.cssText = 'display:flex;gap:12px;align-items:center;margin-top:8px;flex-wrap:wrap;';
                        container.appendChild(controls);
                        VizEngine.createSlider(controls, 'Approximation points', 2, 30, 5, 1, function(v) { numPts.value = Math.round(v); });

                        viz.animate(function(time) {
                            var t = time * 0.001;
                            viz.clear();
                            var ctx = viz.ctx;

                            ctx.fillStyle = viz.colors.white;
                            ctx.font = 'bold 14px sans-serif';
                            ctx.textAlign = 'center';
                            ctx.fillText("Goldstine: J(B_X) is w*-dense in B_{X**}", 280, 22);

                            // Draw B_{X**} (outer ball)
                            var cx = 280, cy = 210, R = 150;
                            ctx.fillStyle = viz.colors.purple + '10';
                            ctx.strokeStyle = viz.colors.purple;
                            ctx.lineWidth = 2;
                            ctx.beginPath();
                            ctx.arc(cx, cy, R, 0, Math.PI * 2);
                            ctx.fill(); ctx.stroke();
                            ctx.fillStyle = viz.colors.purple;
                            ctx.font = '13px sans-serif';
                            ctx.fillText('B_{X**}', cx + R + 15, cy);

                            // Draw J(B_X) (inner region, slightly smaller since X not reflexive)
                            ctx.fillStyle = viz.colors.blue + '15';
                            ctx.strokeStyle = viz.colors.blue;
                            ctx.lineWidth = 1.5;
                            ctx.setLineDash([4, 3]);
                            ctx.beginPath();
                            ctx.arc(cx, cy, R * 0.7, 0, Math.PI * 2);
                            ctx.fill(); ctx.stroke();
                            ctx.setLineDash([]);
                            ctx.fillStyle = viz.colors.blue;
                            ctx.font = '12px sans-serif';
                            ctx.fillText('J(B_X)', cx, cy + R * 0.7 + 18);

                            // Target point Phi in B_{X**} \ J(B_X)
                            var phiAngle = t * 0.3;
                            var phiR = R * 0.85;
                            var phiX = cx + phiR * Math.cos(phiAngle);
                            var phiY = cy + phiR * Math.sin(phiAngle);
                            ctx.fillStyle = viz.colors.orange;
                            ctx.beginPath(); ctx.arc(phiX, phiY, 6, 0, Math.PI * 2); ctx.fill();
                            ctx.fillStyle = viz.colors.orange;
                            ctx.font = 'bold 12px sans-serif';
                            ctx.fillText('\u03A6', phiX + 10, phiY - 10);

                            // Approximating points from J(B_X)
                            var n = numPts.value;
                            for (var i = 0; i < n; i++) {
                                var frac = (i + 1) / (n + 1);
                                // Points from J(B_X) getting closer to Phi
                                var approxR = R * 0.7 * (0.5 + 0.5 * frac);
                                var approxAngle = phiAngle + (1 - frac) * 0.8 * Math.sin(i * 1.3);
                                var ax = cx + approxR * Math.cos(approxAngle);
                                var ay = cy + approxR * Math.sin(approxAngle);

                                // Lerp toward target
                                var lx = ax + (phiX - ax) * frac * 0.6;
                                var ly = ay + (phiY - ay) * frac * 0.6;

                                // Keep inside J(B_X)
                                var dist = Math.sqrt((lx - cx) * (lx - cx) + (ly - cy) * (ly - cy));
                                if (dist > R * 0.7) {
                                    lx = cx + (lx - cx) * R * 0.7 / dist;
                                    ly = cy + (ly - cy) * R * 0.7 / dist;
                                }

                                ctx.fillStyle = viz.colors.teal;
                                ctx.beginPath(); ctx.arc(lx, ly, 3, 0, Math.PI * 2); ctx.fill();

                                // Dashed line from approximation to target
                                if (i === n - 1) {
                                    ctx.strokeStyle = viz.colors.yellow + '60';
                                    ctx.lineWidth = 1;
                                    ctx.setLineDash([3, 3]);
                                    ctx.beginPath(); ctx.moveTo(lx, ly); ctx.lineTo(phiX, phiY); ctx.stroke();
                                    ctx.setLineDash([]);
                                }
                            }

                            // Info
                            ctx.font = '12px sans-serif';
                            ctx.textAlign = 'center';
                            ctx.fillStyle = viz.colors.teal;
                            ctx.fillText('J(x_n) \u2192 \u03A6 in w* topology', 280, 385);
                            ctx.fillStyle = viz.colors.text;
                            ctx.font = '11px sans-serif';
                            ctx.fillText('Every \u03A6 \u2208 B_{X**} is a w*-limit of elements from J(B_X)', 280, 402);
                        });
                    }
                },
                {
                    id: 'viz-reflexivity-characterization',
                    title: 'Reflexivity: When J(B_X) = B_{X**}',
                    description: 'Compare reflexive and non-reflexive spaces through the lens of Goldstine.',
                    setup: function(container) {
                        var viz = new VizEngine(container, { scale: 40, originX: 280, originY: 200 });
                        var isReflexive = { value: false };
                        var controls = document.createElement('div');
                        controls.style.cssText = 'display:flex;gap:12px;align-items:center;margin-top:8px;flex-wrap:wrap;';
                        container.appendChild(controls);
                        var refBtn = VizEngine.createButton(controls, 'Toggle: Non-Reflexive', function() {
                            isReflexive.value = !isReflexive.value;
                            refBtn.textContent = isReflexive.value ? 'Toggle: Reflexive' : 'Toggle: Non-Reflexive';
                        });

                        viz.animate(function(time) {
                            var t = time * 0.001;
                            viz.clear();
                            var ctx = viz.ctx;
                            var refl = isReflexive.value;

                            var cx = 280, cy = 210, R = 140;

                            ctx.fillStyle = viz.colors.white;
                            ctx.font = 'bold 14px sans-serif';
                            ctx.textAlign = 'center';
                            ctx.fillText(refl ? 'Reflexive: J(B_X) = B_{X**}' : 'Non-Reflexive: J(B_X) \u2282 B_{X**}', 280, 25);

                            // B_{X**}
                            ctx.fillStyle = viz.colors.purple + '12';
                            ctx.strokeStyle = viz.colors.purple;
                            ctx.lineWidth = 2;
                            ctx.beginPath();
                            ctx.arc(cx, cy, R, 0, Math.PI * 2);
                            ctx.fill(); ctx.stroke();

                            if (refl) {
                                // J(B_X) fills all of B_{X**}
                                ctx.fillStyle = viz.colors.blue + '25';
                                ctx.beginPath();
                                ctx.arc(cx, cy, R, 0, Math.PI * 2);
                                ctx.fill();
                                ctx.fillStyle = viz.colors.green;
                                ctx.font = 'bold 16px sans-serif';
                                ctx.fillText('J(B_X) = B_{X**}', cx, cy);
                                ctx.font = '12px sans-serif';
                                ctx.fillText('e.g., l\u00B2, L\u00B2, finite-dim', cx, cy + 22);

                                // Scattered points filling the disk
                                for (var i = 0; i < 40; i++) {
                                    var angle = i * 2.399 + t * 0.1;
                                    var r = R * Math.sqrt((i + 1) / 41) * 0.9;
                                    ctx.fillStyle = viz.colors.blue + '80';
                                    ctx.beginPath();
                                    ctx.arc(cx + r * Math.cos(angle), cy + r * Math.sin(angle), 2, 0, Math.PI * 2);
                                    ctx.fill();
                                }
                            } else {
                                // J(B_X) is strictly inside B_{X**}
                                var innerR = R * 0.65;
                                ctx.fillStyle = viz.colors.blue + '20';
                                ctx.strokeStyle = viz.colors.blue;
                                ctx.lineWidth = 1.5;
                                ctx.setLineDash([4, 3]);
                                ctx.beginPath();
                                ctx.arc(cx, cy, innerR, 0, Math.PI * 2);
                                ctx.fill(); ctx.stroke();
                                ctx.setLineDash([]);

                                ctx.fillStyle = viz.colors.blue;
                                ctx.font = '13px sans-serif';
                                ctx.fillText('J(B_X)', cx, cy - 10);
                                ctx.font = '11px sans-serif';
                                ctx.fillText('(dense, not closed)', cx, cy + 8);

                                ctx.fillStyle = viz.colors.purple;
                                ctx.font = '12px sans-serif';
                                ctx.fillText('B_{X**}', cx + R + 15, cy);

                                // Gap region
                                ctx.fillStyle = viz.colors.red + '15';
                                ctx.beginPath();
                                ctx.arc(cx, cy, R, 0, Math.PI * 2);
                                ctx.arc(cx, cy, innerR, 0, Math.PI * 2, true);
                                ctx.fill();
                                ctx.fillStyle = viz.colors.red;
                                ctx.font = '11px sans-serif';
                                ctx.fillText('Gap', cx + R * 0.83, cy - R * 0.4);

                                ctx.fillStyle = viz.colors.text;
                                ctx.font = '12px sans-serif';
                                ctx.fillText('e.g., c_0, l\u00B9, L\u00B9', cx, cy + 28);
                            }

                            // Bottom info
                            ctx.fillStyle = viz.colors.yellow;
                            ctx.font = '12px sans-serif';
                            ctx.textAlign = 'center';
                            ctx.fillText('X reflexive \u21D4 J(B_X) w*-closed \u21D4 B_X weakly compact', 280, 390);
                        });
                    }
                },
                {
                    id: 'viz-goldstine-c0',
                    title: 'Goldstine for c_0: Approximating l-infinity Elements',
                    description: 'See how bounded sequences (in l-infinity) are approximated by sequences converging to 0 (in c_0).',
                    setup: function(container) {
                        var viz = new VizEngine(container, { scale: 30, originX: 50, originY: 280 });
                        var nTerms = { value: 5 };
                        var controls = document.createElement('div');
                        controls.style.cssText = 'display:flex;gap:12px;align-items:center;margin-top:8px;flex-wrap:wrap;';
                        container.appendChild(controls);
                        VizEngine.createSlider(controls, 'Truncation N', 2, 15, 5, 1, function(v) { nTerms.value = Math.round(v); });

                        viz.animate(function(time) {
                            viz.clear();
                            var ctx = viz.ctx;
                            var N = nTerms.value;

                            ctx.fillStyle = viz.colors.white;
                            ctx.font = 'bold 14px sans-serif';
                            ctx.textAlign = 'center';
                            ctx.fillText('Approximating \u03A6 \u2208 B_{l\u221E} by elements of B_{c_0}', 300, 22);

                            // Target: a bounded sequence that doesn't converge to 0
                            // phi_k = (-1)^k * 0.7
                            var numBars = 15;
                            var barW = 28;
                            var spacing = 35;
                            var startX = 60;
                            var baseY = 260;
                            var barScale = 100;

                            // Draw baseline
                            ctx.strokeStyle = viz.colors.axis;
                            ctx.lineWidth = 1;
                            ctx.beginPath();
                            ctx.moveTo(40, baseY);
                            ctx.lineTo(580, baseY);
                            ctx.stroke();

                            for (var k = 1; k <= numBars; k++) {
                                var x = startX + (k - 1) * spacing;
                                var phiVal = Math.pow(-1, k) * 0.7;
                                var approxVal = (k <= N) ? phiVal : 0;

                                // Target bar (outline)
                                var targetH = phiVal * barScale;
                                ctx.strokeStyle = viz.colors.orange;
                                ctx.lineWidth = 1.5;
                                ctx.strokeRect(x, baseY - Math.max(0, targetH), barW, Math.abs(targetH));

                                // Approximation bar (filled)
                                var approxH = approxVal * barScale;
                                ctx.fillStyle = (k <= N) ? viz.colors.blue + '80' : viz.colors.text + '20';
                                if (approxH !== 0) {
                                    ctx.fillRect(x, baseY - Math.max(0, approxH), barW, Math.abs(approxH));
                                }

                                // Label
                                ctx.fillStyle = viz.colors.text;
                                ctx.font = '9px sans-serif';
                                ctx.textAlign = 'center';
                                ctx.fillText(k.toString(), x + barW / 2, baseY + 14);
                            }

                            // Legend
                            ctx.font = '12px sans-serif';
                            ctx.textAlign = 'left';
                            ctx.fillStyle = viz.colors.orange;
                            ctx.fillText('\u03A6 = ((-1)^k \u00B7 0.7) \u2208 l\u221E (target)', 60, 340);
                            ctx.fillStyle = viz.colors.blue;
                            ctx.fillText('x_N = truncation at N=' + N + ' (in c_0)', 60, 360);
                            ctx.fillStyle = viz.colors.teal;
                            ctx.fillText('x_N agrees with \u03A6 on first N coords, then 0', 60, 380);
                            ctx.fillStyle = viz.colors.yellow;
                            ctx.font = '11px sans-serif';
                            ctx.fillText('As N \u2192 \u221E: x_N \u2192 \u03A6 in w* (pointwise)', 60, 398);
                        });
                    }
                }
            ],
            exercises: [
                {
                    question: "Prove Goldstine's theorem for the special case \\(X = c_0\\): show that \\(B_{c_0}\\) is weak*-dense in \\(B_{\\ell^\\infty}\\).",
                    hint: 'Approximate any \\(y \\in B_{\\ell^\\infty}\\) by truncations \\(y_N = (y_1, \\ldots, y_N, 0, 0, \\ldots)\\).',
                    solution: 'Let \\(y = (y_k) \\in B_{\\ell^\\infty}\\) and let \\(y_N = (y_1, \\ldots, y_N, 0, 0, \\ldots) \\in c_0\\). Then \\(\\|y_N\\|_\\infty \\leq \\|y\\|_\\infty \\leq 1\\), so \\(y_N \\in B_{c_0}\\). For any \\(x = (x_k) \\in \\ell^1 = (c_0)^*\\), we have \\(\\hat{x}(y_N) = \\sum_{k=1}^N x_k y_k \\to \\sum_{k=1}^\\infty x_k y_k = \\hat{x}(y)\\) since \\(\\sum |x_k| < \\infty\\). Thus \\(J(y_N) \\xrightarrow{w^*} J(y)\\), showing \\(B_{c_0}\\) is \\(w^*\\)-dense in \\(B_{\\ell^\\infty}\\).'
                },
                {
                    question: 'Use Goldstine\'s theorem to show that \\(X\\) is reflexive if and only if \\(J(B_X)\\) is weak*-closed in \\(X^{**}\\).',
                    hint: 'If \\(J(B_X)\\) is weak*-closed and weak*-dense, what can you conclude?',
                    solution: '(\\(\\Rightarrow\\)) If \\(X\\) is reflexive, \\(J(B_X) = B_{X^{**}}\\) is weak*-compact (Banach-Alaoglu), hence weak*-closed. (\\(\\Leftarrow\\)) If \\(J(B_X)\\) is weak*-closed, then by Goldstine \\(\\overline{J(B_X)}^{w^*} = B_{X^{**}}\\), so \\(J(B_X) = B_{X^{**}}\\). Since \\(J\\) is an isometry, this means \\(J\\) is surjective (every element of \\(B_{X^{**}}\\) is in the range, and by scaling, every element of \\(X^{**}\\) is in the range), so \\(X\\) is reflexive.'
                },
                {
                    question: 'Show that if \\(X\\) is a non-reflexive Banach space, then \\(J(B_X)\\) is not weak*-closed, and hence not weak*-compact.',
                    hint: 'Combine Goldstine with the characterization of reflexivity.',
                    solution: 'By Goldstine, \\(\\overline{J(B_X)}^{w^*} = B_{X^{**}}\\). If \\(X\\) is not reflexive, then \\(J\\) is not surjective, so \\(J(B_X) \\subsetneq B_{X^{**}}\\). Since the closure equals \\(B_{X^{**}}\\) but \\(J(B_X) \\neq B_{X^{**}}\\), we conclude \\(J(B_X)\\) is not weak*-closed. Since it is not closed, it cannot be compact (compact subsets of Hausdorff spaces are closed).'
                }
            ]
        }
    ]
});
